services:
  db:
    image: postgres:16-alpine
    container_name: Mikuro_db
    restart: always
    environment:
      POSTGRES_USER: Mikuro_admin_db
      POSTGRES_PASSWORD: Sergosergo666!
      POSTGRES_DB: Mikuro_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: